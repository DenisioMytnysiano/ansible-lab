[Unit]
Description=Java web application that counts visitors
Requires=network.target
After=network.target

[Service]
Environment="PORT={{ app_port }}"
ExecStart=java -jar {{ app_dir }}/{{ app_file }}
User=root
Restart=always

[Install]
WantedBy=multi-user.target